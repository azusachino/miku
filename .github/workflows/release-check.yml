name: Release check

on:
  workflow_dispatch:
  push:
    tags: ["v*"]

jobs:
  package:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: cachix/install-nix-action@v25
      - run: make check
      - name: Inspect publishable packages
        run: |
          nix develop --command cargo package --list -p miku-domain
          nix develop --command cargo package --list -p miku-markdown
      - name: Verify publish dry runs
        run: |
          nix develop --command cargo publish --dry-run -p miku-domain
          nix develop --command cargo publish --dry-run -p miku-markdown
      - name: Verify private crates are not publishable
        run: |
          test "$(nix develop --command cargo metadata --no-deps --format-version 1 | jq -r '.packages[] | select(.name == "miku-app") | .publish')" = "false"
